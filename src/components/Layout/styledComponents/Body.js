import styled from 'styled-components';

import { Grid } from '@material-ui/core';

export const Body = styled(Grid)`
  display: flex;
  margin: calc(1em + 80px) 0.5em 0.5em 0.5em;
  height: 1800px;
`;
